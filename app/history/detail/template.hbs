<div>
  {{jobs-table jobs=jobs selectedJob=selectedJob order=order orderAction="updateOrder"}}

  <div>
    {{#if selectedJob}}
      {{job-detail job=selectedJob action="goToJob" removeAction="removeJob"}}
    {{else}}
    {{/if}}
  </div>

  {{outlet}}
</div>
