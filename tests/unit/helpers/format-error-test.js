import { formatError } from '../../../helpers/format-error';
import { module, test } from 'qunit';

module('Unit | Helper | format error');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatError(42);
  assert.ok(result);
});
