import {
  formatError
} from 'client/helpers/format-error';

import {module, test} from 'qunit';

module('FormatErrorHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatError(42);
  assert.ok(result);
});
