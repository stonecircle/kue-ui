import { formatJson } from '../../../helpers/format-json';
import { module, test } from 'qunit';

module('Unit | Helper | format json');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatJson(42);
  assert.ok(result);
});
